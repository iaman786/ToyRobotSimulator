namespace ToyRobotSimulation
{
    public class Table
    {
        public int Width { get; }
        public int Height { get; }

        public Table(int width, int height)
        {
            Width = width;
            Height = height;
        }

        // Method to check if a given position is within the table boundaries
       
    }
}
